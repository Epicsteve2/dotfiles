#!/usr/bin/env bash
cp -v "${HOME}/.zshrc"  \
"${HOME}/git/dotfiles/zshrcOsx"

cp -v "${HOME}/.bash_profile" \
"${HOME}/git/dotfiles/bash_profileOsx"

cp -v "${HOME}/.bashrc" \
"${HOME}/git/dotfiles/bashrcOsx"

cp -v "${HOME}/.zshenv" \
"${HOME}/git/dotfiles/zshenvOsx"

cp -v "${HOME}/.p10k.zsh" \
"${HOME}/git/dotfiles/p10kOsx.zsh"